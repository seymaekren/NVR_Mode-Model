function printTen(S, filename)

fid = fopen(filename, 'w');
assert (size(S,1) == 3);
assert (size(S,2) == 3);
for i = 1:3
  for j = 1:3
    fprintf(fid, '%d ', S(i,j));
  end
  fprintf(fid, '\n');
end